# RackTop 版本更新

面向用户的简明更新说明，最新版本位于最上方。


## 1.26.1

- 优化 SSH 终端字体、字号、字重、行高和字距，提升长命令与多列输出的可读性。
- 增加高对比 ANSI 颜色、选区和光标配色，清晰区分提示符、命令和远端程序输出。
- 为 Ubuntu SSH 会话启用 `xterm-256color` 和 truecolor 能力，恢复常见 shell 的彩色提示符与 `ls` 输出。

## 1.26.0

- 新增 Ubuntu 24.04 x64 的 DEB 和 AppImage 桌面安装包。
- 修复 Linux 窗口被当作 macOS 显示的问题。
- 支持通过 Linux 系统钥匙串保存 SSH 密码。
- 支持从 RackTop-Linux 发布页检查并下载新版。

## 1.25.4

- 新增 macOS 与 Windows 应用内自动更新，支持下载、校验、安装并自动重启。
- 新增品牌区独立更新按钮，并保留品牌本身打开“关于 RackTop”的原有行为。
- 新增更新下载进度、已下载大小和安装阶段反馈。
- 支持更新失败后重试或前往 GitHub 手动下载。
- 优化自动更新重启后的窗口激活，避免 RackTop 被其他应用遮挡。
- 优化更新检查，RackTop 每次完整启动都会检查稳定版更新，并沿用不受重启影响的 24 小时后台检查周期。
- 新增关于页当前版本说明入口，可直接查看对应 GitHub Release。
- 新增作者小红书主页入口。
- 支持通过 `ppu-smi` 监控真武 PPU 的设备状态、显存、利用率、温度、功率和计算进程。
- 支持在真武 PPU 服务器上复用现有概览、进程、趋势、任务启动和固定卡终端工作流。
- 新增真武 PPU 网页演示模板，并统一 GPU、NPU 与 PPU 的界面和通知文案。
- 新增侧栏服务器与总览服务器卡的统一右键菜单，可编辑、刷新、打开终端、调整通知或删除服务器。
- 优化服务器通知菜单为“打开、关闭、部分…”两级结构，选择部分后直接进入并展开对应配置。
- 新增“无人占用、无他人占用、不限”三种空闲算力进程筛选模式。
- 修复空闲算力忽略当前用户进程、错误显示本人已占用加速卡为空闲的问题。
- 优化“我的进程”排序为统计实际占用的唯一加速卡，并在用户未手动选择时自动优先显示。
- 优化 GPU、NPU 与 PPU 详情卡的默认收起状态和底部间距，并按设备分别显示 NVIDIA、昇腾与真武 PPU 支持的硬件信息。
- 优化趋势页切换，先显示加载反馈再挂载趋势内容。
- 优化离线服务器操作，提供重新连接、编辑配置和删除服务器三个明确入口。
- 将服务器详情“连接”改为“配置”，并修复 Windows 更新按钮与侧栏右边缘未对齐的问题。
- 移除桌面 WebView 默认右键菜单，避免出现“重新载入”和“检查元素”。

## 1.25.3

- 修复共享存储临时不可达时整台服务器被误报为 SSH 连接超时的问题。
- 支持磁盘采集超时后回退到本地文件系统，保持 CPU、GPU、进程和本地磁盘监控可用。
- 优化刷新和重连状态，保留最近一次有效服务器详情。
- 优化进程枚举与趋势读取，在对应内容区域显示加载状态。
- 支持启动时恢复原有数据库中的最近快照，无需等待首次连接即可查看已有数据。
- 修复没有当前用户任务时 CPU 卡片仍错误显示“你”标记的问题。
- 优化采集成功日志，结构化显示获得的数据及本次发送、接收和写入数据量。
- 新增服务器独立刷新按钮，并将进程与趋势加载状态限制在对应内容区域。
- 支持按服务器打开、关闭或部分保留任务、进程、显存与设备连接通知。
- 修复部分通知保留四类时未自动恢复为打开，以及部分模式可取消最后一类通知的问题。
- 修复选择部分通知后菜单未自动滚入视野及底部留白不足的问题。
- 修复服务器标签无法使用中英文逗号连续添加的问题，并支持按 Delete 退回编辑最后一个标签。

## 1.25.2

- 修复 SSH 在线监控持续在远端 Home 目录创建数字文件的问题。
- 新版读取文件内容并结合名称、所有者、链接数和大小识别旧 Bug 遗留文件，匹配后移入可恢复的 RackTop 私有目录，不直接永久删除。
- 优化遗留文件整理为低优先级后台任务，不阻塞 SSH 监控；首次完整扫描后即写标记，即使没有遗留文件也不再重复检查。
- 优化 Windows 中文安装器，使用 RackTop 应用与卸载图标，并支持自定义安装位置。
- 新增 Windows 卸载时的本机数据选项，默认保留配置和历史记录，勾选后才删除。
- 明确卸载只影响本机，不会删除远端服务器上的项目、数据集、模型或任务日志。
- macOS DMG 保持原生拖拽到“应用程序”的简洁安装流程。
- 修复项目新增服务器时旧副本被重复排队，以及已存在的数据集或模型副本保存后未显示的问题。
- 项目共享优先比较目录差异，纯新增内容直接补齐；需要替换或删除目标内容时仍要求确认。
- 目录联想不再自动补 `/`，总览排序新增“我的进程”，长任务文本改为单行省略。

## 1.25.0

- 新增昇腾 NPU 卡与进程识别，概览和进程区域会按服务器显示 GPU 或 NPU 标签。
- 昇腾启动配置支持选择 NPU 并使用 `ASCEND_RT_VISIBLE_DEVICES`，不改变 NVIDIA 的 GPU 选择和 `CUDA_VISIBLE_DEVICES` 启动逻辑。
- 设置新增默认“标准”和可选“大号”两档字体大小。
- 每 24 小时检查 GitHub Release，点击品牌区可在原关于弹窗中手动检查和查看更新状态。
- 支持忽略当前版本的蓝色更新提示；出现更高版本时提示会自动恢复。
- 优化多卡服务器趋势加载，同一时间范围只读取一次历史数据。
- 支持勾选项目目标、数据集或模型后自动检查目录配置。
- 修复空名称时无法检查配置的问题，并在保存时提供明确的红框校验。
- 修复项目同步暂停后无法可靠继续、并误报目标端已修改的问题。
- 修复添加服务器和添加项目显示校验信息时的表单错位。
- 概览的 GPU/NPU 与 CPU 进程表新增运行时间。
- 修复多目标同步计数遗漏，并将活动目标明确显示为“正在同步中”。
- 优化大文件同步期间的连接状态，短时采集延迟不再让源服务器或目标服务器闪断为离线。
- 支持项目、数据集和模型目录的文件级增量同步，后续更新只传新增、修改和删除项。
- 支持关联仍在同步的数据集或模型，不再误判缺失或重复启动传输。
- 优化关联资源同步反馈，数据集和模型从排队、准备到传输完成均持续显示状态与进度。
- 优化趋势加载，切回页面立即显示缓存并后台刷新，短期趋势查询不再读取完整快照。
- 修复终端多行粘贴被逐行立即执行的问题，整段内容改为确认回车后统一提交。
- 修复无远端进程的任务仍显示运行中，以及结束任务弹窗长期停留的问题。

## 1.24.5

- 新增 SSH Agent 快速配置完成校验，未完成配置时阻止新服务器连接，并对缺少服务器地址或用户名的表单即时标红。
- 支持从服务器表单直接打开 macOS Terminal 或 Windows PowerShell 执行 SSH 配置命令，打开后自动置于前台。
- 保存启动配置后自动关闭配置窗口并返回配置列表，减少重复操作。
- 优化 SSH Agent 初始化后的远端历史常驻进程检查与历史同步重试，降低刚配置完成时的误报。
- 明确区分服务器实时采集状态与远端历史同步失败状态，避免状态提示重复或含义混淆。
- 修复 Windows 快速配置多行命令被拆分以及 macOS 缺少密钥目录的问题，两平台均在真实免密验证成功后才允许保存。
- 修复 macOS 快速配置使用 ssh-copy-id 造成重复连接；改为一次 SSH 写入公钥，并明确提示本机 SSH 代理拒绝连接。
- 优化 macOS Terminal 快速配置展示：先显示可读脚本再执行，不再显示 Base64 编码串。
- 优化远端历史守护进程启动兼容性，并在失败时保留可定位原因的诊断日志。
- 修复保存服务器时等待远端历史初始化导致表单停留在“连接中”；现在先完成保存并进入服务器页面，远端历史在后台初始化。
- 修复远端历史守护脚本在部分 Linux `/bin/sh` 环境中将 `set -eu` 解析为非法选项的问题。
- 修复 Windows 构建将远端 Shell 脚本写成 CRLF，导致 Linux `/bin/sh` 启动守护进程时报 `set: Illegal option` 的问题。

## 1.24.4

- 统一资源历史热力图为 90 天，格子按窗口自适应，时间坐标固定在左侧并默认显示最近时间。
- 修复热力图日期标签冲突、90 天布局过度压缩和左右留白不均问题，并补充存储空间页面标题。
- 修复 Windows 启动时强制最大化的问题，默认窗口调整为更大的 `1500×900` 普通窗口。
- 修复 Windows 顶部深色标题栏和原生菜单栏，窗口控制统一移到通知按钮右侧。
- 修复 Windows Release 启动时伴随命令行窗口的问题。
- 修复 Windows 快捷方式图标白底或黑边，并移除侧栏顶部错误的 macOS 红绿灯留白。
- 修复 Windows PowerShell 无法生成 RackTop 专用 SSH 密钥的问题，并在生成失败时立即给出明确错误。
- 修复没有服务器时五步新手引导不显示的问题。
- 修复 Windows 后台 SSH 检查弹出并反复关闭命令行窗口的问题。
- 修复 Windows 无边框窗口配置和自绘窗口控制按钮失效的问题，macOS 标题栏行为保持不变。

## 1.24.3

- 修正侧栏连续缩放的最小宽度为 220px，兼顾窗口收窄与导航、服务器名称可读性。
- 修复服务器列表遗漏仅有 CPU 进程时的“有你的任务”标记。

## 1.24.2

- 优化侧栏宽度随窗口连续缩放，避免在断点处突然跳变并保持操作文字完整。
- 限制自定义服务器名称最多 24 个字符，防止长名称挤压导航布局。
- 优化概览资源卡六项遥测的单行排列，并在窄容器自动回落为双列。
- 优化概览进程区为“当前 GPU 进程”和“当前 CPU 进程”两个直接区块，移除重复标题。

## 1.24.1

- 统一“显示新手引导”和“显示添加服务器引导”为通用设置中的复选开关。
- 修复关闭新手引导后设置状态未明确显示为未勾选的问题。
- 支持通过保存设置应用新手引导显示状态，取消设置时不改变当前状态。
- 优化两个引导开关的说明，明确五步工作流与 SSH 安全连接提示的覆盖范围。

## 1.24.0

- 新增从服务器接入到启动首个任务的五步新手引导。
- 支持根据真实服务器、资源、项目关联、启动配置和任务记录自动更新引导进度。
- 支持折叠或关闭新手引导，并在设置中随时重新显示。
- 新增网页版新用户状态模拟，方便预览完整入门流程。

## 1.23.0

- 新增项目关联模型，支持像数据集一样长期保存多个模型依赖。
- 支持检查关联模型在项目目标服务器上的副本状态。
- 支持“保存并同步”时只补齐缺失的关联模型副本。
- 项目卡片新增关联模型及其缺失、待检测状态。
- 启动前检查新增关联模型路径和按需同步。
- 修复关联资源缺少目标路径时仍可继续启动任务的问题。
- 支持删除模型或数据集后自动清理项目中的失效关联。

## 1.22.0

- “我的项目”新增模型分类、数量和空状态。
- 支持添加、编辑和移除模型同步对象。
- 支持为模型选择主服务器、目标服务器和独立副本目录。
- 支持模型状态检查、批量同步、单目标同步与暂停/继续。
- 新增 `Llama-3-8B` 模型样例，演示已同步和待创建副本状态。

## 1.21.0

- 新增完整 Shell 启动命令解析，保留环境变量、Conda 和训练命令结构。
- 支持按所选项目和 GPU 自动替换工作目录与 `CUDA_VISIBLE_DEVICES`。
- 新增项目日志路径识别，并将 RackTop 受管日志同步到项目日志位置。
- 新增训练超参数解析和可展开的名称、值编辑列表。
- 新增以项目为主导的启动配置工作区，每个项目单独管理可复用命令和超参模板。
- 支持从项目配置调整本次运行的服务器、项目副本目录、GPU 卡号和超参。
- 新增独立“添加配置”操作，无需立即启动任务即可保存模板。
- 新增默认开启的“是否独占”GPU 筛选。
- 优化启动配置页为项目导航与模板摘要布局，减少命令列截断和无效留白。
- 优化服务器摘要为 GPU 数量与可用/总量系统内存。
- 优化 GPU 摘要为可用/总量显存，并明确标记 MEM。
- 统一资源筛选为 GPU 数量、最低空闲显存和是否独占三列。
- 修复粘贴命令后原始环境变量、日志和训练命令丢失的问题。
- 修复 Conda 参数被误识别为训练超参数的问题。
- 修复启动配置和超参数区域行高过大、信息层级松散的问题。
- 修复“是否独占”与相邻资源筛选项标题和高度不一致的问题。
- 优化启动弹窗宽度，减少超参数和值区域的无效留白。

## 1.20.0

- 统一未关联进程与 RackTop 任务的卡片展示结构。
- 新增按服务器和根 PID 聚合外部进程及子进程。
- 新增将未关联进程关联项目的能力。
- 新增将外部进程命令保存为启动配置的能力。
- 新增未关联进程的终端和安全结束操作。
- 优化项目任务的运行位置，只显示已配置当前项目的在线服务器。
- 优化 GPU 资源选项，取消同一服务器内的型号筛选。
- 优化启动命令预览，显示工作目录、后台运行和日志输出语义。
- 支持从项目和可用 GPU 直接预填任务启动。
- 新增项目与数据集标签页，并优先展示最近执行内容。
- 优化可用 GPU 卡片操作，直接提供启动任务、终端和详情入口。
- 统一任务启动入口，空状态居中显示、有任务时显示在右上角。
- 修复只有未关联进程时页面上方留白过多的问题。
- 修复 GPU 选择卡文本越界覆盖相邻卡片的问题。

## 1.19.0

- 新增跨服务器任务启动与管理能力。
- 新增可复用的启动配置和最近结束记录。
- 支持按项目、服务器和 GPU 要求启动任务。
- 支持查看任务日志、进程关系和实时资源状态。
- 支持安全结束任务及其子进程。
- 保留并集中展示由终端或其他工具启动的未关联进程。
- 优化“我的进程”的字体可读性和信息层级。
- 修复状态轮询可能产生重复 SSH 请求的问题。
- 修复同名命令可能关联到其他用户或其他 GPU 的问题。
- 修复单文件项目被错误当作工作目录的问题。

## 1.18.5

- 修复离线或失败目标可能让项目错误显示“已同步”的问题。
- 优化项目配置顺序，先选择目标服务器，再处理关联数据集。
- 合并重复的路径检查入口，并明确区分检查状态与同步操作。
- 优化项目卡片为只同步待更新目标，避免重复传输已同步副本。
- 修复同名候选目录与真正缺失副本状态混淆的问题。
- 精简主服务器、关联数据集和同步操作的重复说明。

## 1.18.4

- 支持在编辑项目时勾选缺失的关联数据集，并通过底部“保存并同步”一次性补齐。
- 关联数据集补齐只同步缺失服务器，普通“保存”不会触发文件传输。
- 修复重新检测后仍保留已经失效的数据集补齐计划的问题。

## 1.18.3

- 修复 macOS 内置终端无法使用 `Command + V` 粘贴的问题。
- 恢复编辑菜单中的撤销、重做、剪切、复制、粘贴和全选。
- 新增主服务器用户名与 IP 信息，便于识别同步来源。
- 优化项目配置弹窗，让路径和检测状态获得稳定的显示空间。
- 统一目标服务器为带横向分隔线的紧凑列表模块。
- 修复持续空闲筛选因资源与进程采样频率不同而错误显示 0 张可用 GPU 的问题。
- 修复项目配置弹窗无法滚动和未选目标服务器无法选择的问题。
- 优化项目配置为安静的中性分隔布局，减少整行蓝色和绿色强调。

## 1.18.2

- 修复项目重新检测时没有持续显示检测中动画的问题。
- 优化重新检测按钮，使其在远端检测完成前保持明确的忙碌状态。

## 1.18.1

- 优化项目和数据集卡片为稳定的左右双列排列。
- 优化项目来源摘要为主服务器、数据量和最近内容修改三列。
- 优化项目与数据集编辑弹窗，让地址获得更多可用宽度并取消横向溢出。
- 优化长路径默认显示文件名一端，并支持使用方向键查看前级目录。
- 新增“重新检测”的旋转图标与检测中反馈。
- 修复页面刷新导致地址输入框失去焦点的问题。
- 修复远端目录联想加载时地址输入自动取消选中的问题。

## 1.18.0

- 新增项目同步的实时进度、速度、剩余时间和传输阶段。
- 新增单台目标同步的暂停与断点续传。
- 新增目标端内容变更检测和覆盖前二次确认。
- 新增目标磁盘空间预检与中断任务恢复。
- 优化目录同步为完整副本安全切换，避免中断后出现新旧内容混合。
- 优化同步全部与项目检查的并发数，减轻服务器网络和磁盘压力。
- 优化项目卡片的颜色层级、三列来源摘要和右对齐操作列。
- 优化项目配置的目标路径宽度、路径联想和键盘焦点操作。
- 修复目标内容被修改后仍显示已同步的问题。
- 修复首次同步后关系状态仍显示目标缺失的问题。
- 修复保存项目关系时隐式修改关联数据集配置的问题。
- 修复文件传输不完整时仍可能发布的问题。
- 修复同步取消后 SSH 子进程未及时回收的问题。
- 修复危险目录、相互包含目录和同名目录被自动误选的问题。

## 1.17.3

- 修复删除主服务器时项目或数据集配置被一并删除的问题。
- 支持为失去主服务器的项目或数据集重新选择同步来源。
- 修复删除目标服务器后项目列表和目标关系未及时更新的问题。
- 保留项目与数据集的附属关系，并确保服务器文件不受删除操作影响。
- 修复目标目录默认使用项目名称的问题，改为沿用主目录的父目录和名称。
- 优化目标服务器列表为清晰的独立圆角行。
- 新增项目源目录与目标副本的最近内容修改时间和上次同步时间。
- 支持识别通过 SSH 终端或其他工具完成的源目录修改。
- 新增项目卡片“同步全部”操作。
- 优化大文件同步为返回项目列表后在后台执行，并持续显示同步状态。
- 修复项目与数据集之间出现大块空白的问题。
- 新增关联数据集的同名路径检查，明确提示缺失或多个匹配。
- 支持添加项目时按数据集选择首次同步，并提供全选操作。
- 支持进入项目页时检查主服务器最新内容时间，各目标保留独立的上次同步时间。
- 取消项目与数据集的周期轮询，避免大目录持续产生远程磁盘遍历负担。
- 修复小文件数据量被显示为 `0.0 MB` 的问题。

## 1.17.2

- 修复持续一小时筛选因只加载 30 分钟历史而始终显示零张可用 GPU 的问题。
- 支持在整个持续时间内匿名检查其他用户的 GPU 进程占用状态。
- 支持从最近三小时旧快照自动恢复匿名占用历史。
- 支持远端持续保存匿名 GPU 占用状态，同时兼容既有历史数据。

## 1.17.1

- 修复“我的项目”内容紧贴侧栏的问题，排版与寻找空闲算力保持一致。
- 修复远端 `~/...` 路径检测和同步到错误目录的问题。

## 1.17.0

- 新增“我的项目”页面，统一管理跨服务器项目与数据集。
- 支持为每个同步对象指定一台主服务器和多台目标服务器。
- 支持自动查找各服务器 Home 目录内的同名文件或目录。
- 支持输入目录时按当前服务器实时联想远端路径。
- 支持为每台目标服务器单独修改同步路径。
- 支持项目关联零个或多个数据集，并双向展示附属关系。
- 关联数据集不会随项目自动同步，仍由用户单独触发。
- 支持一键将主服务器内容安全同步到指定目标服务器。
- 新增项目优先、数据集在后的响应式瀑布流布局。
- 支持仅移除同步配置而不删除服务器上的文件。

## 1.16.1

- 修复外观设置中同层级标题出现黑灰两种颜色的问题。

## 1.16.0

- 新增紧凑和扩展两种 macOS 菜单栏状态模式。
- 支持在扩展模式中用图标和数字显示待处理预约与异常进程。
- 支持从菜单栏直接打开预约中心或“我的进程”。
- 优化菜单栏状态更新，避免闪烁和计数变化导致的宽度跳动。
- 修复手动结束进程被误认为意外退出的问题。
- 修复 1 天和 3 天 GPU、CPU 趋势将长期分桶误判为缺失数据的问题。
- 统一 1 天和 3 天趋势为 10 分钟平均线与最低、最高极值带。
- 新增右上角手动重连的服务器完成进度提示。
- 统一菜单栏模式选项与其他设置项的字号和字重。
- 修复 GPU 或 CPU 趋势切换到 1 天、3 天时界面卡顿的问题。
- 优化实时刷新期间的历史图表，避免未变化的数据重复计算。

## 1.15.6

- 新增 RackTop、服务器、查看、窗口和帮助五组 macOS 中文应用菜单。
- 支持从系统菜单添加服务器、导入 SSH Config、刷新服务器和切换主要页面。
- 恢复当前 macOS 提供的完整中文原生窗口管理功能。
- 新增使用说明与项目主页菜单入口。

## 1.15.5

- 修复首次采集与远端历史同步并发时服务器先显示超时、随后才上线的问题。
- 修复 SSH Agent 模式仍使用残留私钥路径的问题。
- 支持指定私钥时仅尝试所选密钥。
- 修复非 SSH Agent 认证方式仍显示密钥快速配置的问题。
- 修复密码输入框与系统钥匙串选项高度不一致的问题。
- 优化首次连接为先显示基础状态、随后补齐进程和磁盘数据。
- 优化远端历史同步进度，近期已同步的服务器直接计为完成。
- 修复未输入密码也能保存密码认证服务器的问题。
- 修复系统钥匙串授权被拒绝后在轮询或重启时持续重复弹窗的问题。
- 支持在服务器详情页手动重新连接时再次请求钥匙串授权。
- 将“保存到系统钥匙串”改为默认勾选并移除重复图标。
- 修复非密码认证服务器仍可能在 App 启动时触发系统钥匙串密码框的问题。

## 1.15.4

- 修复 NVIDIA 多项查询叠加时超过 15 秒被误判为服务器断线的问题。
- 将完整采集超时保护调整为 30 秒。

## 1.15.3

- 修复采集 SSH 控制会话失效后持续超时的问题。
- 优化快速配置代码块的固定尺寸与自动换行。

## 1.15.2

- 修复连接中和离线服务器详情页缺少删除入口的问题。
- 统一无采样服务器的侧栏状态信息显示。

## 1.15.1

- 修复服务器持续连接时无法删除服务器的问题。
- 修复服务器离线或密码凭据暂时不可读时无法删除本地服务器记录的问题。
- 修复 SSH Agent 密钥过多时快速配置无法写入 RackTop 公钥的问题。
- 将密码登录引导中的推荐方式改为 SSH Agent。

## 1.15.0

- 新增可独立撤销的 RackTop 专用 SSH 密钥。
- 支持删除服务器时选择撤销 RackTop 配置的免密登录。
- 支持远端离线时在 24 小时清理重试中继续撤销免密授权。

## 1.14.12

- 修复空闲算力条件栏不显示已选 GPU 型号的问题。
- 修复空闲算力条件栏不显示已选 CPU 型号的问题。
- 修复空闲算力条件栏不显示已选服务器标签的问题。

## 1.14.11

- 将最近三小时趋势完整保留为原始精度。
- 支持长期趋势数据保留平均值、最低值和最高值。

## 1.14.10

- 统一 GPU 提醒的忽略周期。
- 支持 GPU 异常消失后自动清除忽略记录。
- 支持同一 GPU 异常再次发生时重新提醒。

## 1.14.9

- 修复历史同步完成后顶部状态仍持续旋转的问题。
- 支持在连接页查看已忽略的显存占用预警。
- 支持在连接页恢复已忽略的显存占用预警。

## 1.14.8

- 修复服务器离线后详情页仍显示旧资源数据的问题。
- 支持点击 GPU 异常图标查看具体问题。
- 统一详情页和通知中的服务器名称表达。

## 1.14.3

- 优化“我的进程”的操作布局。
- 优化“我的进程”的结束进程反馈。
- 修复终端可用区域未占满窗口的问题。
- 修复共享 GPU 告警不准确的问题。
- 修复单张异常 GPU 导致其他 GPU 进程无法采集的问题。

## 1.14.2

- 扩展服务器搜索范围，支持连接信息、硬件信息和标签组合搜索。
- 简化进程列表的任务标识。
- 简化进程列表的结束按钮。

## 1.14.1

- 统一总览、进程页和“我的进程”的任务展示。
- 统一总览、进程页和“我的进程”的结束操作。
- 新增长时间占用显存但 UTL 为零的 GPU 预警。
- 修复进程仍存活时错误提示结束成功的问题。
- 修复部分服务器 GPU 进程漏报的问题。
- 修复进程详情无法收起的问题。

## 1.14.0

- 新增日志数据审计视图，集中展示发送、接收、写入、本地占用和失败数量。
- 支持按服务器查看实际获得的数据。
- 支持按服务器查看最近执行的命令。

## 1.13.0

- 将服务器交互日志改为当前运行周期内的实时 SSH 命令流。
- 优化“我的进程”的服务器分组。
- 优化“我的进程”的进程过滤。
- 优化“我的进程”的空状态。
- 统一资源热力图与 GPU 使用分布的响应式切换时机。

## 1.12.1

- 将算力总览中的最近刷新信息替换为不可读取 GPU 数量。

## 1.12.0

- 新增历史数据分层压缩以降低数据库增长速度。
- 支持压缩历史数据时保留短时峰值。

## 1.11.0

- 新增“我的进程”并按服务器集中展示任务。
- 支持在“我的进程”中管理当前用户的 GPU 进程。
- 支持在“我的进程”中管理当前用户的 CPU 主进程。

## 1.10.0

- 新增服务器交互日志以集中查看采集状态。
- 支持在服务器交互日志中查看当前关联进程。

## 1.9.0

- 新增 GPU 显存长期占用但计算利用率为零的应用内预警。

## 1.8.2

- 修复单张 NVIDIA GPU 异常导致整台服务器 GPU 数据消失的问题。
- 支持按服务器忽略 GPU 异常提醒。
- 支持按服务器恢复 GPU 异常提醒。
- 修复趋势页加载反馈滞后的问题。
- 修复资源热力图缩放异常的问题。

## 1.8.1

- 修复存储空间误识别系统挂载和容器挂载的问题。
- 区分当前用户、其他用户和空闲容量的存储颜色。
- 优化 GPU 使用分布的网格密度。
- 调整趋势页的内容顺序。
- 优化 macOS 侧栏拖动排序体验。

## 1.8.0

- 新增存储空间趋势，以 200 格华夫饼展示每个有效磁盘的占用情况。
- 支持删除服务器时同步清理本地数据。
- 支持删除服务器时同步清理远端采集进程。
- 支持删除服务器时同步清理远端 RackTop 数据。
- 支持远端离线时保留清理任务并在 24 小时内自动重试。

## 1.7.2

- 优化 GPU 使用分布华夫饼的空间利用率。
- 支持点击空闲算力卡片直接打开终端。
- 支持从空闲算力卡片直接创建预约。
- 优化 SSH 快速配置提示。
- 调整新增服务器的默认历史保存策略。
- 新增侧栏服务器拖动排序。

## 1.7.1

- 修复部分 Linux 服务器无法连接远端历史的问题。
- 重新划分本地 App 与远端守护进程的历史采集职责。
- 优化概览进程表的字段排版。
- 为概览进程表提供更充足的长命令显示空间。

## 1.7.0

- 新增远端 GPU 使用分布的持久保存。
- 支持重新打开 App 后增量同步 GPU 使用分布。
- 新增持久 SSH 终端。
- 新增 GPU 固定快捷终端。
- 新增短期资源趋势分析。

## 1.6.0

- 新增远端历史同步进度提示。
- 新增远端历史导入数量提示。
- 新增远端历史同步失败提示。

## 1.5.1

- 修正历史热力图中 UTL 与 MEM 的指标配色。

## 1.5.0

- 新增远端历史持续采集，在桌面端关闭期间每分钟保存轻量资源样本。
- 支持重新打开 App 后将远端历史增量同步到本地。

## 1.4.0

- 新增 GitHub 热力图风格的 CPU 与 GPU 资源历史视图。
- 支持每张资源卡片独立切换 UTL 指标。
- 支持每张资源卡片独立切换 MEM 指标。
- 新增 GPU 详情中的总 SM 利用率。

## 1.3.0

- 新增空闲算力条件预约。
- 新增预约条件稳定满足后的系统通知。
- 分离高频状态刷新与历史曲线采样。
- 新增 GPU 进程显示。
- 新增 CPU 主进程显示。
- 新增 GPU 与 CPU 进程的父子关系显示。
- 修复空闲显存边界判断错误的问题。
- 修复无标签卡片与有标签卡片高度不一致的问题。

## 1.2.0

- 新增服务器物理位置，方便定位需要现场处理的设备。
- 修复滚动条侵入顶部区域的问题。
- 修复 macOS 窗口无法连续拖动的问题。
- 修复状态墙右侧留白过大的问题。
- 提升空闲条件的字号可读性。

## 1.1.0

- 支持保存空闲算力筛选条件。
- 支持空闲算力筛选条件在侧栏和结果页之间保持同步。

## 1.0.1

- 扩大 macOS 窗口可拖动区域。
- 支持连续拖动 macOS 窗口。
- 完善服务器删除流程。
- 完善 SSH Config 导入流程。
- 新增重复连接识别与提示。

## 1.0.0

- 发布多服务器 GPU 监控能力。
- 新增跨服务器资源总览。
- 新增空闲 GPU 发现能力。
- 修复实时趋势图 X 轴标签过密的问题。
- 使用容量光标显示当前用户占用的 GPU 显存。

## 0.2.0

- 将默认总览升级为全部服务器的 GPU 状态墙。
- 重构服务器概览的信息层级。
- 重构 GPU 详情的信息层级。
- 重构趋势图的信息层级。
- 修复总览瀑布流布局异常的问题。
- 修复服务器排序后布局未正确重排的问题。
- 修复 macOS Dock 图标显示异常的问题。
- 修复窗口缩小时内容裁切的问题。
- 修复后台刷新造成界面反复反馈的问题。

## 0.1.1

- 修复双服务器监控的显示异常。
- 修复遥测数据越界的问题。
- 修复多项界面交互问题。
- 提升 GPU 状态墙的稳定性。
- 提升趋势图的稳定性。
- 提升日志页面的稳定性。
- 提升 SSH 连接页面的稳定性。

## 0.1.0

- 发布首个无需远端 Agent 的多服务器 GPU 监控版本。
- 支持通过本机 OpenSSH 查看 GPU 状态。
- 支持通过本机 OpenSSH 查看 CPU 状态。
- 支持通过本机 OpenSSH 查看系统负载。
- 支持通过本机 OpenSSH 查看进程信息。
- 支持通过本机 OpenSSH 查看历史趋势。
- 支持快速发现空闲 GPU。
